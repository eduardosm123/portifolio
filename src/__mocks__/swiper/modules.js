module.exports = {
    Navigation: ({ children }) => <div>{children}</div>,  // Simula o componente Swiper
    Pagination: ({ children }) => <div>{children}</div>,  // Simula o componente SwiperSlide
  };